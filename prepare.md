# Prepare — martech-trend-agent 決策記錄

> **版本**：MT-003 · 2026-07-20（待決策事項 1 筆，見文末）
> 記錄規則繼承根 `prepare.md`，只寫本專案差異。編號前綴 `MT-`。

## 決策日誌

### MT-003 · 2026-07-20 · 分析層與半自動排程
- **決策**（用戶確認）：新增「分析專區」——見解由 Claude 親自寫（非 Python 模板）；每 3 天更新
- **為何不用 Python 模板**（用戶理由，2026-08-02 補記）：這份報告要的是**對當前狀況的判斷**，
  不是把統計數字套進固定句型。模板產出的是格式正確但沒有資訊量的段落——
  要有用的分析就需要模型現場讀過材料再寫，這也直接推出下方「為何非全自動」
- **架構**：機械層（run.py 抓取統計，本機 launchd 每 3 天自動＋桌面通知）／
  分析層（`/martech-report` 由 Claude 就地寫見解＋更新 artifact＋寫 Notion）＝半自動
- **為何非全自動**：見解需模型在場，本機無法定時喚醒模型；雲端 routine 存取不到本機資源、
  抓取有被擋風險。棄全雲端。
- **通知落點演進**：原訂 Email → Gmail 連接器只能建草稿且授權範圍不足（insufficient scopes）
  → 改 **Notion**：母 page「MarTech 趨勢報告」`3a3e6df2-9bcf-81f8-9e10-cc9ce907924e`，
  每期一個子 page
- **落地**：`.claude/commands/martech-report.md`、`scripts/*.sh`

### MT-002 · 2026-07-20 · 主軸修正：應用趨勢為主、職缺為次
- **決策**（用戶指示）：報告主軸＝新聞/文章的應用趨勢判斷；職缺降為次要節（公司需求訊號）
- **落地**：新聞源 5→9（加 ChiefMartec/MarTech Series/Appier Blog/Insider Blog）、
  主題分類 8→12 類、每主題附代表文章、趨勢比較新增主題聲量升降；
  feed 支援 `keepAll`（純 MarTech 源全收）與 `maxAgeDays`（低頻源放寬時間窗）

### MT-001 · 2026-07-19 · 職缺來源選擇
- **決策**：職缺來源＝Greenhouse 官方 API（Appier）＋ Yourator；104 不納入
- **理由**：104 搜尋端點已上 Cloudflare bot 防護（實測 403／挑戰頁），規避防護不做；
  Greenhouse 為官方公開 API 且含完整內文，資料品質反而更好
- **代價**：91APP／iKala 等只在 104 刊登的公司職缺數會是 0，報告已標注此限制
- **關聯**：← D-001（本專案為求職資訊基礎設施，服務 Appier 投遞主線）

### MT-000 · 2026-07-19 · 專案建立
- 生活線作品「Martech 情報 pipeline」落地為本專案；範圍：台灣職缺＋國際新聞、
  手動執行為主、報告 markdown＋HTML artifact（三項均經用戶確認）
- **為何「手動執行為主」**（用戶理由，2026-08-02 補記）：
  1. **資訊時效**——排程跑出來的資料與實際讀報告的時點會有天數落差，趨勢判斷吃這個落差
  2. **本機排程在電腦沒開時不會執行**——launchd 只在機器醒著才觸發，
     排程看似自動、實際常常空跑或整期漏掉，仍要人回頭補，等於多付成本沒買到可靠性
- **與 MT-003 的關係**：MT-003 的「半自動」是這條原則的落地形狀——
  機械層可以排程（漏了重跑成本低），分析層維持手動（要模型在場，且時效敏感）

---

## 待決策事項

> 這一節與上方「決策日誌」的差別：上方是**已經拍板**的，這裡是**還沒拍板但不該再拖**的。
> 分開放是因為把未決事項寫成決策，下一期讀的人會以為它已經定案。

### 待決 1 · 新聞層的定位（2026-08-19 提出）

**問題**：報告第 1 節「應用主題聲量」自稱是本報告的主指標（← MT-002），
而分析層已經**連續三期**（08-10／08-13／08-19）以同一個理由禁止判讀它。
每期都在產出一節不能用的內容，再花一段解釋它為什麼不能用。

**兩個已知病灶**（2026-08-19 均已定位到具體位置，不是模糊的「分類粗糙」）：

1. **樣本不連續**：新聞每期重抓 feed 當下內容，快速源（iThome／TechOrange／INSIDE／
   MarTech.org／MarTech Series／MarketingDive）**六期以來跨期重疊一直是 0**。
   慢速源會撐高總重疊數（08-19 為 14／60），但那不是新聞的連續性。
2. **中文子字串誤命中**：`config.yaml:129` 的 `個資` 會在「修補 58 **個資**安漏洞」中命中，
   量詞「個」黏上了「資安」的「資」。08-19 那期「隱私/法規」9 篇裡只有 1 篇真的談法規。
   同型的還有 `第三方`（第三方外掛）與 `上市`（新車上市，`config.yaml:130`）。

**A 案 — 修**：新聞改累積式儲存（維護 `data/news-store.json`，每期併入新抓到的、
依 `maxAgeDays` 過期，主題聲量改由 store 計算）＋ 收斂關鍵詞比對（中文詞至少要能擋掉
上述三個子字串誤命中）。**代價**：要決定歷史七期快照怎麼處理（重算或標為舊口徑）。

**B 案 — 降級**：承認新聞層做不到跨期比較，把主題聲量從「主指標」降級為
「本期閱讀清單」，報告文案與 MT-002 的主軸敘述跟著改。**代價**：等於放棄這個工具的主指標。

**目前傾向 A**（根因已縮到三個具體關鍵詞，修正成本比原本以為的低），
但這是決策，需用戶拍板後才動手。
