# Changelog

格式依循 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.0.0/)。

## [Unreleased]

### Added
- **2026-08-31 第 8 期資料快照**：職缺 143 筆（Greenhouse 82＋Yourator 61）、新聞 66 篇，
  失敗來源 0 個。機械層報告 `reports/report-2026-08-31.md`，`latest.md` 同步
- **第 8 期分析** `reports/analysis-2026-08-31.md` 與 `reports/artifact-2026-08-31.html`
- **職缺面新增第三條過濾規則**：判讀「新增」時，**刊登日早於第一份快照（2026-07-19）
  且八期從未出現過的一律不算新開編制**。Greenhouse 的 `first_published` 不會因重新上架
  而更新，本期 Appier 的 6 筆新增裡就有 3 筆屬此類（刊登日 2025-03-02／2025-11-11／2026-01-29）

### Changed
- **上期把 2019 年三個常駐缺寫成「不動的基本盤」，本期破了**：
  `gh-1501746 Machine Learning Scientist`（2019-01-10 刊登、連七期都在）本期消失，
  同期下架的還有 `gh-1623669 Campaign Management Intern`（2019-04-01）。
  Appier 台北刊登於 2024 年前的缺從 **10 個掉到 8 個**。
  改立的說法：**常駐缺不是不會變的背景值，每期照樣逐筆比對**
- **Appier AI／ML 職八期新低**：15→15→17→19→17→18→18→**14**（台北 8→8→10→11→9→9→9→**7**），
  且本期新增 0 筆 AI／ML 職。**但消失的四筆裡有一筆是 Appier 清自己的重複刊登**
  （`gh-7092458` 與 `gh-7789533` 同職稱同城市長期並存），真正減少三筆。
  同期總職缺也掉 4 筆，縮減幅度與整體同向，故**不寫成「在收 AI 編制」，標「推測」**
- **待決 1 的建議改成三步**（原本是 A／B 二選一）：① 先在 `news.json` 每篇多存
  `matchedTerms` 欄位 → ② 再收斂關鍵詞 → ③ 最後才考慮累積式儲存。
  前兩步小到不太需要拍板，第三步才是真正要決定的

### Fixed
- 尚無程式修正。`config.yaml` 自 2026-07-20 起未動，`個資` 誤命中本期原封不動再犯一次
  （〈Apache 發布 Tomcat 更新，修補 11 個資安漏洞〉又被歸入「隱私/法規」）

### Notes
- **本期查出一個新的工具缺陷：分類根因無法回查**。快照的 `summary` 只存前 500 字
  （出處：`fetch/newsRss.py:58`），而分類跑在完整內文上。本期「行銷自動化」13 篇裡
  **有 5 篇的命中詞不在快照內**，事後無從查證。
  **分類粗糙可以修，分類不可回查連「修得對不對」都驗不了**——這是待決 1 新增前置步驟的理由
- **但也實測到一個好消息**：把 `自動化`／`automation` 從關鍵詞移除後重跑過濾，
  **本期一篇文章都不會被濾掉（0 篇）**。該清單同時兼任主題計數與相關性守門
  （出處：`fetch/newsRss.py:51`），原本擔心「收斂關鍵詞會縮小語料庫」對這個詞不成立
- **跨期禁令第五期沿用，且本期是最乾淨的一次驗證**：間隔從 6 天**翻倍到 12 天**，
  重疊卻從 14／60 掉到 **4／66**——方向相反。4 篇全來自慢速源
  （Appier Blog 3、ChiefMartec 1），六個快速源重疊**第五期維持 0**
- **awoo 從 0 跳到 9 筆，並把 `SEO/GEO` 寫進職稱**：職缺編號 `ytor-48066`～`48074` 連號，
  是近期一次刊登的一批（三種角色 × 三個城市）。`SEO/GEO` 這個字串**八期以來第一次出現**，
  且開了明寫「初階」的工程職。本期 66 篇新聞裡沒有任何一篇談 GEO／AEO——
  **這是本期唯一一個職缺跑在新聞前面的訊號**。技能榜 `SEO` 1→9 完全由這一家造成
- **上期三個檢查項的答案**：① `gh-8118867` 第三期仍在、標題與內文都沒再動
  （「兩週內下架＝迅速補到人」沒發生，9/10 滿一個月時可結案）；
  ② 非 Senior 池掉回 4，**但機制與預測的不同**——掉的是 2019 那個 ML Scientist，
  `gh-8118867` 還在，**方向猜對、機制猜錯**；③ `MT-` 決策仍未立
- **薪資本期沒有任何一組可用**：`數據科學/ML` 從 1 筆掉到 **0 筆**（那筆隨職缺消失）。
  `行銷/客戶成功` 18 筆與上期 17 筆樣本不同批，`軟體工程` 3 筆全部換人，皆不可相減

### Added
- **2026-08-19 第 7 期資料快照**：職缺 144 筆（Greenhouse 86＋Yourator 58）、新聞 60 篇，
  失敗來源 0 個。機械層報告 `reports/report-2026-08-19.md`，`latest.md` 同步
- **第 7 期分析** `reports/analysis-2026-08-19.md` 與 `reports/artifact-2026-08-19.html`
- **`prepare.md` 新增「待決策事項」小節**：目前一筆——新聞層的定位要修（A）還是降級（B）。
  這條連三期被分析層以同一理由否決跨期判讀，已經不是每期加免責聲明能處理的事

### Changed
- **撤回上期才立的「資深 vs 初階」框架**。推翻它的是上期用來立論的同一筆職缺：
  `gh-8118867` 本期把標題的 `Senior` 拿掉（`Senior Digital Advertising Data Analyst`
  → `Digital Advertising Data Analyst`），而 JD 內文 **2501 對 2501 字元、逐行 diff 零差異**。
  加上 Appier 台北非 Senior 的 AI／資料職七期為 3→3→4→5→4→4→**5**（本期為高點），
  「初階在收」不成立。**縮小後仍成立的只有一筆**：`gh-7907856`
  （非 Senior 的 ML Scientist LLM & Agents）連三期缺席
- **改立一條七期都支持的保守說法**：Appier 台北職缺結構極穩定，2019 年的三個常駐缺
  （`gh-1501724`／`gh-1501726`／`gh-1501746`）七期全在，真正的異動每期只有 0–2 筆。
  要判斷它在招什麼，只能看那 0–2 筆

### Fixed
- 尚無程式修正。本期查出的兩個問題（分類器誤命中、新聞跨期）都列為待決策，未動 `config.yaml`

### Notes
- **主題分類誤判查出根因，不再是「分類粗糙」的模糊說法**：關鍵詞 `個資`
  （`config.yaml:129`）在「修補 58 **個資**安漏洞」裡命中——中文沒有詞界，
  純子字串比對把量詞「個」黏上「資安」的「資」。本期「隱私/法規」9 篇裡
  **只有 1 篇真的談法規**，5 篇是這樣進來的漏洞通報。
  同型誤判：`第三方`（指第三方外掛）、`上市`（指新車上市，`config.yaml:130`）。
  多重匹配也惡化——上期最嚴重是三重，本期一篇同時命中 **7 個主題**
- **新增一條新聞面判讀規則（累積至兩條）**：「連兩期同向才算趨勢」**不適用於新聞面**。
  本期形式上通過該規則的有 CDP（+1→+3）與廣告投放（−2→−3），一樣不判讀
- **重疊率上升但禁令更強**：本期重疊 14／60（上期 5／66），但 14 篇全部來自慢速源，
  六個快速源重疊仍是 0——間隔從 3 天拉到 6 天也沒動過
- **上期的兩個檢查項都有答案**：① `gh-8118867` 還在，但降級了（見 Changed）；
  ② Appier 實習缺維持 8 筆且**逐筆與上期同一批**，沒有任何 AI／研究類實習回來
  ——「Appier 台北沒有 AI 研究實習缺」這條維持成立
- **方法層的自我修正**：連兩期都用「一筆職缺的異動」去描述一間公司的策略轉向，
  然後下一期就被推翻。Appier 台北 53 筆缺，單筆佔比不到 2%，撐不起「轉向」這種敘述
- **與下游的「交叉驗證」是假的**：上期兩份報告「各自獨立走到同一個結論」的說法有誤——
  兩邊當時用的是**同一筆證據**（同一個 `gh-8118867`）。兩個工具給出同一個結論時，
  要先確認它們用的不是同一筆資料

---

> 以下為同屬 Unreleased 的 **2026-08-13（第 6 期）** 條目。兩期都是資料與報告刷新、
> 未動程式，因此不發版；分隔線只用來標示期別，不代表已釋出。

### Added
- **2026-08-13 第 6 期資料快照**：職缺 145 筆（Greenhouse 88＋Yourator 57）、新聞 66 篇，
  失敗來源 0 個。機械層報告 `reports/report-2026-08-13.md`，`latest.md` 同步。
- **第 6 期分析** `reports/analysis-2026-08-13.md`
- **`reports/artifact-2026-08-13.html` 進 git**——先前各期的 artifact 只存在雲端，
  repo 裡沒有副本，等於**無法重現任何一期的頁面**。本期起改為與下游
  `martech-job-radar` 一致，把產出的 HTML 一併收進版控

### Changed
- **修正一個講了四期的框架**：「研發重心從資料移到 AI」不成立。
  同一期 Appier 台北新開 `Senior Digital Advertising Data Analyst`（08/10 刊登，
  六期以來第一個新開分析職），而初階 AI 研究職連兩期缺席。
  真正一致的分界是**年資**（資深在開、初階在收），不是職能

### Notes
- **上期的觀察項有一條被推翻**：「實習類整體在收」不成立，本期 Appier 實習 6 → 8。
  但回升的兩筆是 `Account Operations Intern`（營運端，且刊登日為 2021／2025 的舊缺重新上架），
  AI 研究類實習仍然一個都沒有。修正為「收的是 AI 研究類實習，不是實習整體」
- **主題聲量的跨期禁令證據更強**：本期重疊 **5／66**，且間隔從 5 天縮到 3 天
  重疊率並未變高——重疊取決於 feed 長度而非時間間隔。`topicDelta` 本期一律不判讀
- **新增一條職缺判讀規則**（累積至兩條）：異動要依序濾掉 ① 非追蹤公司
  ② 刊登日遠早於本期者（重新上架非新編制，本期 2 筆）。
  原擬的第三條「換城市／換頁造成的一增一減」**當日撤回**——查明那是下游
  `martech-job-radar` 整併鍵的 bug（無職缺編號時退回比職稱），不是真實現象。
  本專案以 `jobId` 為鍵，不受影響
- 本次刷新是由下游 `martech-job-radar` 的第 3 期報告觸發的：
  下游讀的是本專案的**工作區**，先前工作區停在 `docs/version-stamp-third`，
  最新快照是 08-10。先刷新本專案再跑下游，下游才不會觸發新鮮度警告

## [1.2.1] - 2026-08-10

### Added
- `docs/ARCHITECTURE.md`：四層管線圖、模組職責、識別鍵與跨期比較是否成立、對下游的六欄契約
- README 新增判讀限制：**主題聲量不可跨期比較**（見下方 Notes）

### Fixed
- **RSS 抓取無逾時上限**：`feedparser.parse()` 不吃 timeout 參數，來源掛住連線會讓程序永不結束；
  排程情境下 launchd 不會在前次仍在跑時啟動新的一次，等於**靜默停擺且無任何錯誤訊息**。
  於 `run.py` 入口設 `socket.setdefaulttimeout(30)`（requests 自帶的 20/30 秒更短，優先生效）
- `scripts/refresh.log` 加入 `.gitignore`——排程一跑就產生，本專案 commit 流程用 `git add -A`，
  否則會被一起提交進版控

### Notes
- **查出主題聲量的測量方式不支援跨期比較**（2026-08-10 第 5 期分析）：
  `fetch/newsRss.py` 只讀 feed 當下的 `entries`，`maxAgeDays` 只能剔除舊文、無法找回已滑出
  feed 的文章，也沒有跨期累積儲存。實測相鄰兩期文章重疊數為 9、13、4、6（分母 31～66），
  能跨期存活的幾乎只有低頻源的舊文。因此 `stats.json` 的 `topicDelta` 是兩批幾乎不相交樣本
  之間的差，**不是趨勢**。
- 要修得把新聞改成累積式儲存（`data/news-store.json`，每期併入新抓到的、依 `maxAgeDays` 過期），
  這會讓歷史五期快照無法直接沿用，**屬於需要先立一筆 MT 決策的改動**，本版不動程式碼，
  只把限制寫進 README 與 `docs/ARCHITECTURE.md`
- 職缺面不受影響：差集以 `jobId` 為鍵，同 id 改標題不會製造假異動（本期實測 `gh-6820898`）

## [1.2.0] - 2026-07-20

### Added
- **分析層**：`/martech-report` 指令（`.claude/commands/martech-report.md`）——Claude 讀完資料
  親自撰寫「分析專區」（跨來源見解、對求職的意涵），非模板；產出 `reports/analysis-*.md`
- 首份分析專區 `reports/analysis-2026-07-20.md`
- **半自動排程**：`scripts/refresh.sh`（機械層刷新＋桌面通知）、`install-schedule.sh` /
  `uninstall-schedule.sh`（macOS launchd，每 3 天）
- 視覺化 artifact 新增分析專區（三大重點＋見解卡）
- **通知落點**：Notion 母 page「MarTech 趨勢報告」，每期一個子 page（含見解＋artifact 連結）

### Changed
- README 使用方式改為「機械層／分析層」兩層架構，新增定期更新（半自動）說明

### Notes
- 通知原訂用 Email，但 Gmail 連接器只能建草稿且授權範圍不足，改用 Notion（見 MT-003）

## [1.1.0] - 2026-07-20

### Changed
- **主軸修正**：報告改為「應用趨勢（新聞/文章）為主、職缺為次」的結構（MT-002）
- 應用主題分類 8 → 12 類（新增 對話式商務／零售媒體／行銷自動化／個人化推薦 等）
- 趨勢比較新增「主題聲量升降」，為報告最重要的趨勢訊號

### Added
- 新聞源 5 → 9：ChiefMartec、MarTech Series、Appier Blog、Insider Blog
- feed 選項 `keepAll`（純 MarTech 源不經關鍵詞過濾）、`company`（公司部落格自動掛標記）、
  `maxAgeDays`（低頻高價值源放寬時間窗）
- 報告新增「各主題代表文章」節（1a），趨勢判斷不只看數字

## [1.0.0] - 2026-07-19

### Added
- 抓取層：Greenhouse 官方 Job Board API（Appier，含職缺內文）、Yourator 搜尋 API（台灣新創，僱主過濾）、5 個新聞 RSS 源
- 統計層：公司職缺數、職稱分類、技能關鍵詞頻率、薪資樣本中位數、台灣可投遞清單、跨快照趨勢比較
- 報告層：繁中 markdown 週報（每節附判讀指引），`reports/latest.md` 同步更新
- `--no-jobs` / `--no-news` / `--reanalyze` 執行選項

### Removed
- 104 抓取模組——端點有 Cloudflare bot 防護，不做規避，改以 Greenhouse＋Yourator 為職缺來源
